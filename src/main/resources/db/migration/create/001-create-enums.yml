databaseChangeLog:
  - changeSet:
      id: 001-create-enums
      author: ivan_k
      changes:
        - sql: |
            CREATE TYPE card_status AS ENUM ('CREATED', 'ACTIVE', 'BLOCKED', 'EXPIRED');
            CREATE TYPE block_ticket_status AS ENUM ('PENDING', 'APPROVED', 'REJECTED');
